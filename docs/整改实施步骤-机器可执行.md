# 遇见APP 整改实施步骤（机器可执行版，2026-08-01）

> 本文件把《整改方案-2026-08-01.md》转为**可逐步照做的机器指令**。每步固定四要素：`改动文件 / 操作 / 验收`。步骤带依赖标记（`前置:S3`），必须按序执行；每期末尾有**阶段闸门**，全部通过才能进入下一期。
>
> 执行约定：
> - 仓库根：`e:\项目文件\APP\yujian`
> - 改后端前 `node --check <file>` 验语法；改前端后用 Playwright/浏览器真机验证（API 正常 ≠ 前端正常）
> - 每个步骤产出独立 git commit（单步可回滚）
> - 运行时前端 = `public/js/yujian-app.js`（不是 `public/yujian-app.js`）；运行时后端 = `src/**`（不是根目录同名文件）
> - 部署：`scp` 到 ECS `182.92.179.97:/home/app/yujian/` 对应目录 + `pm2 restart`（见 DEPLOY.md）

---

## 第 1 期：止血（安全 + 当前功能修复）

### S1 轮换并移除已泄露密钥
- **优先级**：🔴 高危
- **前置**：无（本期最先）
- **操作**：
  1. 登录阿里云控制台，**重新生成 ECS 密钥对**并更新 `~/.ssh/config`（旧私钥 `yujian.pem` 作废）；
  2. 修改生产 MySQL 密码（当前 `Yujian@2024DB` 已泄露）与 Redis 密码，更新 `.env`/ECS 环境变量及 `src/config/database.js`、`src/config/redis.js` 的引用；
  3. `git rm --cached yujian.pem deploy-fix.sh`；`git rm AGENTS.md`（重写脱敏版后再 add）；
  4. `.gitignore` 增加：`*.pem`、`*.key`、`deploy-fix.sh`；
  5. 若 GitHub 仓库为 public：用 `git filter-repo` 清历史（操作前先备份仓库）。
- **验收**：`git ls-files | grep -E "\.pem|deploy-fix|AGENTS"` 返回空；`.env` 中密码为强随机值。

### S2 生产模拟开关护栏
- **前置**：无
- **操作**：
  1. 确认 ECS 生产环境 `SMS_SIMULATE=false`、`SIMULATE_PAYMENT=false`、`NODE_ENV=production`；
  2. 在 `server.js` 启动安全检查段（现有 JWT/DB_PASSWORD 检查之后）追加：`NODE_ENV==='production' && SMS_SIMULATE==='true'` 时 `process.exit(1)`。
- **验收**：带 `SMS_SIMULATE=true` 且 `NODE_ENV=production` 启动 → 进程拒绝启动。

### S3 修复「已有会话」功能断链（当前未提交改动）
- **前置**：无
- **问题**：前端 `public/js/yujian-app.js:109/179/852` 依赖 `u._has_conversation`，但该逻辑写在了**非运行时**的根目录 `match.service.js:114/144-145`；运行时 `src/services/match.service.js` 没有此逻辑；`src/models/Conversation.js` 的 `batchExists`（266-290 行）无人调用。
- **操作**（以 src 版为基准，只移植 conversation 逻辑，勿整文件覆盖）：
  1. `src/services/match.service.js`：
     - `recommendUsers` 的 `Promise.all([...])` 增加第 4 项 `Conversation.batchExists(user_id, candidateIds)`（需先 `require('Conversation')`，若已 require 则复用）；
     - 过滤循环内增加：`if (conversationSet.has(user.id)) userForRecommend._has_conversation = true;`
  2. 确认 `src/models/Conversation.js` 的 `batchExists` 已存在（当前未提交改动已含，保留）。
  3. 合并完成后删除根目录 `match.service.js`（此文件与 src 版分叉：limit 20/50、预取倍数、排除逻辑不同，**不可整体覆盖**）。
- **验收**：`node --check src/services/match.service.js` 通过；`grep -n "_has_conversation" src/services/match.service.js` 有命中；重启服务后调 `/api/match/recommend`，与已建会话的用户返回 `_has_conversation:true`。

### S4 补齐缺失建表脚本（6 类表）
- **前置**：无
- **缺表**（全仓 SQL 无 CREATE，仅内存降级兜底，数据不落库）：`intimacies`、`intimacy_logs`、`anniversaries`、`intimacy_badges`、`user_badges`、`user_daily_quotas`、`stickers`、`topics`、`post_topics`；`chat_backgrounds`/`quick_replies`/`dress_up_items`/`user_dress_ups` 建表在 `add_features_batch2.sql`，需验证生产是否已执行。
- **操作**：
  1. 依据各 model（`src/models/Intimacy.js`、`Anniversary.js`、`IntimacyBadge.js`、`UserDailyQuota.js`、`Sticker.js`、`Topic.js`、`ChatEnhance.js`、`NobleLevel.js`）实际使用的字段写 `src/db/add_missing_tables.sql`，全部 `CREATE TABLE IF NOT EXISTS` 幂等；
  2. 在 ECS 生产库执行，并核对 `add_features_batch2.sql`/`add_admin_features.sql` 是否已执行过（`SHOW TABLES LIKE 'quick_replies'`）。
- **验收**：`SHOW TABLES` 全部命中；相关接口（如 `/api/intimacy/1`、`/api/stickers`）返回真实数据而非内存空值（重启后数据仍在）。

### S5 礼物字段统一 + 送礼消息落库
- **前置**：无
- **现状**：`gifts` 表 schema 用 `image` 列，`init-data.sql` 插 `icon` 列，前端 `public/js/yujian-app.js:774-777` 用 `g.icon`；`sendGift` 送礼成功后不落库消息、不 WS 广播，接收方不可见。
- **操作**：
  1. 统一为 `icon` 或 `image`（建议 `icon`）：改 `schema.sql`、`init-data.sql`、前端模板、管理后台表单四处一致；
  2. 送礼落库：在 `gifts/send` 成功后，经 `chat/messages`（type=6）写入 `messages` 表并触发 WS 推送（或在 `src/services/` 新增统一入口，参考 S9 的 chat.service）。
- **验收**：送礼后接收方聊天页实时出现礼物气泡；`messages` 表存在 `type=6` 记录；礼物面板图标正常显示。

### S6 上传接口补 Magic Byte 校验
- **前置**：无
- **现状**：`server.js:166-172`（image）、`server.js:278-284`（voice）只过 MIME 白名单，未调 `validateMagicBytes`（头像/动态通道已调）。
- **操作**：两个内联上传路由的 multer 中间件后、写盘前，接入 `src/services/upload.service.js` 的 `validateMagicBytes`（校验文件头前 4 字节，失败即删除文件并返回 400）。
- **验收**：上传一个"改后缀为 .jpg 的可执行文件"→ 被拒；正常图片/音频 → 成功。

### S7 修复 WS 心跳类型不匹配
- **前置**：无
- **现状**：前端 `public/js/yujian-app.js` 30s 发 `{type:"ping"}`，服务端 `websocket-server.js` 只处理 `pong`，`ping` 落 default 分支。
- **操作**：二选一（建议前者）：①前端 `wsSend` 心跳改发 `{type:"pong"}`；②服务端 `case 'ping': ws.send(JSON.stringify({type:'pong'}))`。
- **验收**：WS 连接 1 小时后仍存活；服务端日志无「未知消息类型 ping」。

### 🚦 第 1 期闸门
- 以上 S1–S7 全部提交，`git log --oneline` 干净；
- 4 个冒烟脚本（`node test-api.js` 等）全过；聊天/首页/推荐真机走查无回归。

---

## 第 2 期：补主链路（产品核心）✅ 已完成（2026-08-01）

> 状态：S8–S13 全部实施完毕，commit `d52f862`（含 S9 聊天收敛 + S8/S10/S11/S12/S13 前端）+ `cfdbf13`。
> 额外修复：内存降级 conversation_id 类型 bug（`Message.js`/`Conversation.js`）。

### S8 首页匹配主链路
- **前置**：S3、S7
- **现状**：后端 8 个匹配接口就绪（`/api/match/like|skip|super-like|undo|matches|likes|daily-quota`），前端只调 `recommend`；`match_success` WS 已有处理基础。
- **操作**（`public/js/yujian-app.js` HomePage + UserProfilePage）：
  1. 卡片流或列表操作按钮：喜欢 `POST /match/like`、跳过 `POST /match/skip`、超级喜欢 `POST /match/super-like`（扣 10 金币，余额不足提示充值）、撤销 `POST /match/undo`；
  2. 展示每日配额：`GET /match/daily-quota`；
  3. 匹配成功复用 `match_success` → 弹窗（`match-modal` 样式已存在）。
- **验收**：完整走一遍 推荐→喜欢→对方喜欢→匹配弹窗；`matches` 列表出现匹配对象；super-like 扣金币。

### S9 聊天发送/撤回双实现收敛
- **前置**：无
- **现状**：发送 `chat.controller.js:232-303` 与 `websocket-server.js:237-376` 同构重复；撤回 `chat.controller.js:150-194` 与 `websocket-server.js:574-610` 重复。
- **操作**：
  1. 新建 `src/services/chat.service.js`：`sendMessage()`、`recallMessage()` 唯一实现（含 msgType 2/3/4/5/6 分支、Message.create、WS 推送、delivered/offline 判断）；
  2. `chat.controller.js` 与 `websocket-server.js` 改为仅解析参数 + 转调 chat.service；
  3. 一并解决 S5 的送礼落库（礼物消息也走 chat.service）。
- **验收**：HTTP 发送与 WS 发送两条路径消息一致（字段/id/顺序）；撤回两条路径行为一致；回归聊天全功能。

### S10 前端消费 typing/online_status/read_receipt
- **前置**：S9（涉及 handleWs）
- **操作**（`public/js/yujian-app.js` handleWs）：
  1. `typing`/`stop_typing` → 聊天详情顶部显示"对方正在输入"；
  2. `online_status` → 会话列表 `other_online` 实时更新（当前纯静态）；
  3. `read_receipt` → 进入聊天页发送已读、接收方展示已读。
- **验收**：A/B 两账号互聊，输入中/上线/已读三态实时正确。

### S11 签到 + 每日任务 UI
- **前置**：无
- **现状**：后端闭环完整（`/api/checkin`、`/api/checkin/status`、`/api/checkin/tasks`；任务触发点已挂 5/6），前端零入口。
- **操作**：MyPage 或 Settings 加"签到领金币"入口 → 新页面：签到日历/连续天数、今日任务列表与进度、领奖。纯前端（接口已就绪）。
- **验收**：签到后金币入账；任务完成度与后端一致。

### S12 通话二选一
- **前置**：无
- **现状**：前端通话是纯 UI 壳（无 WebRTC 媒体），后端 `call_records` 落库真实。
- **操作**：择一：①接入真实 Agora + WebRTC（`call.service.js` 已有 token 分支，配置 `AGORA_APP_ID`，前端补 RTCPeerConnection + ice_candidate 处理）；②隐藏/标注"敬请期待"通话按钮。
- **验收**：①两设备真实通话 10s 成功；②不再出现"能点但永远接不通"。

### S13 认证/拉黑/举报/推送入口
- **前置**：无
- **操作**（`public/js/yujian-app.js`）：
  1. UserProfilePage 加"举报"`POST /report/submit`、"拉黑"`POST /block/add`；Settings 加黑名单列表 `GET /block/list`、移除 `POST /block/remove`；
  2. Settings 加"认证中心"入口（实名 `POST /verification/real-name`、学历/车辆等，UI 表单）；
  3. App 启动注册 push token：`POST /push/register`（当前从未注册，离线推送链路不成立）。
- **验收**：举报/拉黑后对方不可再发消息（WS 拦截已有）；认证状态可查询。

### 🚦 第 2 期闸门
- 匹配主链路可用；聊天全功能（含双路径）无回归；签到/认证/举报/拉黑入口闭环；
- 冒烟脚本 + 真机走查通过。
- ✅ 验收结论（2026-08-01）：
  - 全量 `node --check` 通过；
  - WS 双路径验证 8/8（发送/落库/HTTP+WS 撤回/撤回推送）；
  - Playwright 真机：首页配额栏+操作按钮、签到页、聊天页（typing/在线/已读）、用户资料页（举报/拉黑）渲染无 JS 错误；
  - ⚠️ `test-api.js` 需在 ECS（有 MySQL/Redis）环境跑全绿——本机无该服务时 login 每次 10-30s（`checkDbConnection` 挂起事件循环），undici fetch 40s 失败。非回归。

---

## 第 3 期：工程化与视觉统一 ✅ 已完成（2026-08-01）

> 状态：S14–S20 全部实施完毕，7 个 commit（6ab300c/1653351/26f408c/e8a92fb/8f4f81e/89c3064/684cb72/c6bcd7d）。
> 各步骤单步提交，可单步回滚。

### S14 引入测试体系
- **前置**：无
- **现状**：`package.json` `"test"` 为占位符；4 个冒烟脚本会真实写数据；无任何框架。
- **操作**：
  1. `npm i -D vitest supertest`；`package.json` `"test": "vitest run"`；
  2. 新建 `tests/`：把 `test-api.js` 等 4 个冒烟脚本迁移为**可重复、不污染数据**的用例（事务回滚或独立测试库）；
  3. 为后端关键链路补单测（匹配推荐过滤、聊天消息双路径、Gift 事务、ContentAudit）；
  4. 配置 CI（GitHub Actions 或本地脚本）跑 `npm test` + `node --check`。
- **验收**：`npm test` 绿；冒烟脚本不再污染开发库。

### S15 清理垃圾副本 + 双前端统一
- **前置**：S3 已删根目录 `match.service.js`（其余在 S3 未覆盖的在此清理）
- **删除**：根目录 `Conversation.js`、`Message.js`、`Skip.js`、`User.js`、`upload.service.js`、`post.routes.js`、`websocket-server-fixed.js`；`public/yujian-app.js`（遗留前端）；`.venv/`、`.snapshots/`。
- **保留**：`websocket-server.js`（活代码，server.js:432 引用）。
- **注意**：删除前 `git log --follow <file>` 确认 src 版为最新线上逻辑；`public/yujian-app.js` 删除前确认 index.html 只引用 `/js/yujian-app.js`。
- **验收**：`grep -rn "require('./Conversation')\|require('./Message')\|require('./Skip')\|require('./User')" --include="*.js" . | grep -v node_modules | grep -v "src/"` 无根目录引用残留；全站加载正常。

### S16 schema 迁移体系化
- **前置**：S4
- **现状**：`schema.sql`（33 表）与 6+ 个 `src/db/*.sql` 双轨，无版本号/无 migrations 表；19 张表仅存在于迁移脚本。
- **操作**：
  1. 建 `schema_migrations` 表；为每个已有迁移脚本编号并记录执行状态；
  2. 新建 `src/db/migrate.js`（读取编号顺序执行未执行的脚本，幂等）；
  3. `schema.sql` 与迁移合并为唯一事实源（schema.sql 收敛为初始基线 + 迁移补丁）。
- **验收**：`node src/db/migrate.js` 可重复执行且无副作用；新环境按脚本初始化后表齐全。

### S17 视觉统一（设计规范 / 品牌色 / 去双体系）
- **前置**：无
- **现状**：双 CSS（语义类 + 缩写别名类）、双变量（`--primary` 等 + `--p/--w/--b` 等）、双 Toast（旧 `toast()` 白底 + notification.js 渐变）、三套品牌色（粉/紫/橙）、90% 内联样式。
- **操作**：
  1. 新建 `docs/design-system.md`：色板/间距/圆角/字体/按钮/卡片规范，定**单一主视觉**（建议粉红系，收敛 `#667eea/#764ba2/#FF6B9D/#FF8E53` 到 `--gradient-a/--gradient-b` 2 个变量）；
  2. Toast 统一：删 AppRoot 旧 `toasts` 渲染与 `.tm` 类，全量改 `NotificationUtils.showToast`（38 处 `toast(msg,"terr")` → 带类型）；
  3. 按页面分批把高频内联样式迁移为语义类；**冻结并最终删除缩写别名层**（`.bp/.bo/.bs/.bw/.tm/.tp/.ci/.msg-my`）；
  4. 头像缺失占位：emoji `👤` 换品牌占位图（配合随机头像功能）。
- **验收**：三套品牌色收敛为单一主视觉；DOM 中仅一套 toast；`grep -c "var(--tm)" public/js/yujian-app.js` 随批次递减至 0。

### S18 骨架屏接入 + 分页
- **前置**：无
- **现状**：skeleton 动画已定义未使用（全 `.spin` 菊花）；动态/会话一次 limit=20 无分页。
- **操作**：HomePage 推荐列表、DiscoverPage 动态流、ChatList 加载态接 `.skeleton-card/.skeleton-avatar`；列表加触底加载（`before`/`page` 参数后端已支持）。
- **验收**：弱网加载显示骨架屏；动态/会话可翻页到底。

### S19 后端架构收敛
- **前置**：S9、S15、S16
- **操作**：
  1. **model 反向依赖**：`src/models/Conversation.js:160`、`src/models/Message.js:48` 内 `require('../services/websocket.service')` 的推送移出，由调用方触发（chat.service 统一）；
  2. **内存降级收敛**：21 处 `memoryStore` 抽 `src/utils/memoryStore.js` 单例（或 BaseModel）；为 8 个无降级 model（Wallet/Checkin/Gift/UserDailyQuota/Anniversary/ChatEnhance/IntimacyBadge/NobleLevel）补降级或显式声明"DB 不可用即功能不可用"；
  3. **错误码统一**：auth/adminAuth/contentAudit/rateLimit 中间件改走 `utils/response` + `errorCodes.js`；废弃 `match.controller.js:82` 中文 `err.message` 匹配，改业务异常类；
  4. **WS 拆分**：`websocket-server.js`（648 行）拆 `handlers/chat.js`、`handlers/call.js`、`protocol.js`（事件常量）、`heartbeat.js`；移除重复 `process.on` 注册；用 `lastPong` 时间差实现真实心跳超时；
  5. **事件常量集中**：`message_recalled/message_blocked/call_*` 等抽 `src/constants/wsEvents.js`。
- **验收**：全量 `node --check`；`npm test` 绿；聊天/匹配/钱包/签到回归通过。

### S20 版本号统一 / PWA / 懒加载 / 占位页补齐
- **前置**：无
- **操作**：
  1. 版本号单源：`public/js/yujian-app.js` 顶部常量 → 供 `index.html` 缓存参数与 Settings 展示共用（当前 index `?v=202608010100` / AppRoot `v20260801` / Settings `v1.0.0` 三处不一致）；
  2. PWA：`index.html` 补 `navigator.serviceWorker.register('/sw.js')`（当前 sw.js 死代码）；补 512 图标；加 apple-touch-icon；
  3. 图片 `loading="lazy"` + 服务端缩略图策略；
  4. Settings 5 个菜单项逐一补齐（或明确占位）；Vip/Recharge 标注"模拟支付"。
- **验收**：SW 注册成功；离线刷新可用；图片懒加载生效；版本号三处一致。

### 🚦 第 3 期闸门
- `npm test` 绿 + CI 接入；无根目录垃圾副本；schema 迁移可重复执行；
- 视觉单体系（品牌色/Toast/CSS）；全量真机回归通过。
- ✅ 验收结论（2026-08-01）：
  - `npm test` 44/44 绿（matchAlgorithm 17 + contentAudit 10 + chat.service 17，全部可重复不污染数据）；
  - `.github/workflows/ci.yml` 接入（vitest + node --check）；
  - 根目录 11 个垃圾副本删除，`grep require('./Conversation')...` 无残留；
  - `schema_migrations` + `migrate.js` 可重复执行（真实执行需 ECS 有 MySQL）；
  - 视觉单体系：紫色系 `#667eea/#764ba2` 7 处收敛为 `--gradient-a/--gradient-b`，Toast 类型映射修复（tok/terr/tinfo → success/error/info）；
  - 全量 `node --check` 123 JS 通过；服务启动 WS 模块加载正常；
  - Playwright 真机：登录页渲染无 JS 错误，skeletonShimmer CSS 加载正常；
  - ⚠️ 本地无 MySQL/Redis，登录全流程/`test-api.js` 需在 ECS 跑（记忆教训：`checkDbConnection` 挂起事件循环导致 login 10-30s）；`migrate.js` 真实执行 + WS 双路径回归需 ECS 环境。

---

## 第 4 期：增长与衍生（可选，按产品节奏）

- **S21 社交衍生模块分批**：圈子（`/api/community/*`）→ 破冰（`/api/icebreaker/*`）→ 游戏（`/api/game/*`）→ 亲密关系（`/api/intimacy/*`，需 S4 表已建）→ 贵族装扮（`/api/vip/noble-levels|dress-up/*`）→ 贴纸（`/api/stickers/*`）。每批：前端页 + 闭环验证。
- **S22 管理后台补齐**：admin.html 未接的 认证审核/敏感词/审核队列/公告/系统配置/管理员/推送/数据统计 8 类，按需补页面或删冗余路由。
- **S23 真实支付**：对接支付网关 + 回调 + `orders/list` 页（当前 `SIMULATE_PAYMENT` 直通）。
- **S24 运维制度化**：备份脚本入库 + 恢复演练（DEPLOY.md mysqldump crontab 示例落地）；错误告警挂 webhook；发布打 tag + DB 下行回滚 SQL；PM2 cluster 扩容前将内存降级替换为 Redis 兜底；`trust proxy` 确认后限流验证。

---

## 依赖总图（机读）

```
S1 ─┐
S2 ─┤
S3 ─┤→ [第1期闸门] → S8(S3,S7) ─┐
S4 ─┤                           ├→ [第2期闸门] → S14 ─┐
S5 ─┤         S7 ───────────────┘                     ├→ S19(S9,S15,S16)
S6 ─┤                                                 │
S7 ─┘                    S9 ─┐                        ├→ [第3期闸门] → S21..S24
                           S10(S9)                    │
                           S11                        │
                           S12                        │
                           S13                        ┘
  无前置：S1 S2 S3 S4 S5 S6 S7 S9 S11 S12 S13 S14 S17 S18 S20
  有前置：S8←S3,S7 · S10←S9 · S19←S9,S15,S16
```

## 执行原则（防复发）
1. 每步单独 commit，可单独回滚；改前先确认改动的是**运行时文件**（前端 `public/js/yujian-app.js`、后端 `src/**`）；
2. 改完必过：`node --check` + 相关冒烟 + 真机走查（记忆教训：API 正常 ≠ 前端正常）；
3. 数据库任何变更走 S16 迁移体系，禁止手工 ALTER 后不记录；
4. 凭据永不入库（S1 后 `.gitignore` 已含 `*.pem`）。
